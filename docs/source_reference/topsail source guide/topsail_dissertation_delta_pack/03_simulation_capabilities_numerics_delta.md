# 03 - Simulation capabilities and numerical implementation delta

This file extracts dissertation-only numerical guidance. It deliberately does not repeat the paper pack's basic boundary-condition taxonomy, except where the dissertation adds code-facing detail.

## Computational and numerical guidance

| Item | Source location and short quote | Actionable Codex instruction | Diagnostic/test to add | Matters for |
|---|---|---|---|---|
| CSS convergence criterion is a normalized squared L2 difference of cycle initial states. | Diss p. 215-216 (PDF p. 250-251), §4.2.5. Quote: "squared L2-norm of the difference" | Implement/report `e_t = (1/n_x) * ||x_t - x_{t-1}||_2^2`; default absolute tolerance near `1e-8` only if current code/source agrees. | `test_css_metric_matches_dissertation_formula`; report `css_error_by_cycle.csv`. | Both |
| CSS may use the full initial-condition vector or selected adsorber states. | Diss p. 216 (PDF p. 251), §4.2.5. Quote: "pick state variables related to adsorption columns" | Make CSS state mask explicit in params/output. Do not silently switch between full-state and adsorber-only convergence. | `test_css_state_mask_full_vs_adsorber_only`; output `css_state_mask.json`. | Both |
| CSS error should generally decrease if the simulation is well put together. | Diss p. 216 (PDF p. 251), §4.2.5. Quote: "will continuously decrease" | Plot/log CSS error by cycle and flag non-monotone excursions without automatically failing. | `plot_css_convergence.m`; `test_css_convergence_report_written`. | Both |
| Event-driven simulations can reduce cycles to CSS, but this is preliminary and mode-dependent. | Diss p. 234-236 (PDF p. 269-271), §5.5.3. Quote: "often require significantly fewer cycles" | Compare fixed-duration vs event-driven convergence only after identical accounting. Do not promise acceleration. | `test_event_vs_time_css_report_has_cycle_counts`; include pressure-driven caveat. | Both |
| Pressure-driven simulations did not show the same event-driven CSS acceleration trend. | Diss p. 236 (PDF p. 271), §5.5.3. Quote: "did not observe the trend" | Keep event-acceleration claims scoped to tested flow-driven cases. | Add report field `css_acceleration_mode_supported=false` for untested/pressure-driven cases. | Both |
| Multi-adsorber event criteria can conflict; no general solution is given. | Diss p. 235-236 (PDF p. 270-271), §5.5.3. Quote: "not aware of any general approach" | Require an event-priority manifest for any step with more than one active bed/objective. | `test_multi_bed_event_conflict_requires_policy`. | Four-bed |
| Example conflict policy: Kayser prioritised repressurisation over feed breakthrough. | Diss p. 236 (PDF p. 271), §5.5.3. Quote: "giving precedent to the repressurization event" | Implement event priorities as source-specific data, not a universal rule. | `test_event_priority_is_manifested_not_hardcoded`. | Four-bed |
| Flow reversals can occur briefly in well-tuned cycles. | Diss p. 229 (PDF p. 264), §5.5.1. Quote: "even in well-tuned cycles" | Treat reversal as diagnostic data. Do not automatically fail a run because an interior flow changes sign. | `test_flow_reversal_logged_not_failed`. | Both |
| Simplified sign-fixed balances violate mass/energy during reversals. | Diss p. 230 (PDF p. 265), §5.5.1. Quote: "mass and energy balances would be violated" | Never replace pseudo-flow logic with `v_plus=v`, `v_minus=0` for a whole step. | `test_negative_interior_flow_conserves_component_balance`. | Both |
| Flow-driven reversals can arise from adsorption sinks, not just pressure gradients. | Diss p. 230 (PDF p. 265), §5.5.1. Quote: "rate of adsorption may be quite high" | For no-pressure-drop mode, diagnose reversals using adsorption-rate and boundary-flow ledgers, not pressure-gradient intuition. | `plot_flow_reversal_with_adsorption_rate.m`; `test_reversal_report_includes_adsorption_sink_terms`. | Schell |
| No-pressure-drop mode uses LPs, but guess-and-check avoids most direct LP solves. | Diss p. 226-228 (PDF p. 261-263), §5.4. Quote: "LP is only solved in 10%" | Track LP recourse fraction per step and fail only if unexpectedly high for a known benchmark. | `test_lp_recourse_fraction_reported`; output `lp_recourse_by_step.csv`. | Both |
| Guess-and-check can give 3x-4x speedups in listed flow-driven cases. | Diss p. 228 (PDF p. 263), §5.4. Quote: "speed-up of ~4x" | Preserve the fast path. Any refactor must compare outputs and recourse counts against baseline. | `test_guess_check_fast_path_matches_lp_path_on_small_case`. | Both |
| Pressure-driven Jacobians are sparse because adjacent CSTR flows depend only on adjacent states. | Diss p. 228 (PDF p. 263), §5.4. Quote: "Jacobian matrices ... are sparse" | Provide or preserve `JPattern`/sparsity pattern for pressure-driven ode15s steps. | `test_pressure_driven_jpattern_dimensions`; benchmark RHS evaluations before/after. | Both |
| Sparse Jacobian patterns matter more as `n_c` grows. | Diss p. 228 (PDF p. 263), §5.4. Quote: "improvement is much greater when nc is larger" | Four-bed/high-node tests should include JPattern regression, not just correctness. | `test_large_nc_jpattern_available`. | Four-bed |
| Dynamic step model is a Hessenberg index-1 semi-explicit DAE but solved as ODE with inner algebraic flow solves. | Diss p. 204-205 (PDF p. 239-240), §4.2.4. Quote: "Hessenberg index-1 system" | Keep algebraic flow calculation inside RHS for existing models. Do not add algebraic states casually. | `test_state_vector_excludes_algebraic_flow_rates`; `test_funcVol_called_per_rhs`. | Both |
| Current ODE route may be replaced by DAE solvers for implicit models. | Diss p. 244 (PDF p. 279), §6.2.4. Quote: "use a Differential Algebraic Equation (DAE) solver" | Treat DAE work as a separate feature branch tied to implicit adsorption models. | Stop condition: do not modify Schell routing to chase DAE work. | Research-only |
| Pressure-driven models compute volumetric flows directly; no recourse needed for reversals. | Diss p. 210 (PDF p. 245), §4.2.4. Quote: "handle the flow directions automatically" | Keep Ergun/Kozeny-Carman flow code vectorized and sign-aware. | `test_pressure_drop_flow_sign_change_no_lp`. | Both |
| No-pressure-drop varying-pressure steps fall back to `linprog` when assumed directions fail. | Diss p. 211 (PDF p. 246), §4.2.4. Quote: "we solve the LP" | Ensure Optimization Toolbox or replacement LP solver is available. Log fallback reason. | `test_no_pressure_drop_lp_fallback_on_forced_reversal`. | Both |
| All units use CIS adsorber normalization constants. | Diss p. 165 (PDF p. 200), §3.7.1. Quote: "adopt normalization constants that belong to CSTRs-in-series" | Non-Excel manifests must supply dimensional values and call the same scaling path as Excel imports. | `test_manifest_scaling_matches_excel_scaling`; `test_tank_volume_scaled_by_adsorber_volume`. | Both |
| Time is normalized by adsorber residence time `tau = epsilon V_b / v_0`. | Diss p. 165 (PDF p. 200), §3.7.1. Quote: "residence time in an adsorber" | Do not normalize step durations by tank volumes or cycle time. | `test_step_duration_dimensionless_roundtrip`. | Both |
| Cumulative moles and energy have explicit scales. | Diss p. 189-190 (PDF p. 224-225), §3.7.6. Quote: "set N* = c0v0tau" | Ledger exports must identify dimensional vs dimensionless cumulative quantities. | `test_cumulative_flow_energy_dimensional_roundtrip`. | Schell |
| Numerical diffusion is tied to cell averaging and node count. | Diss p. 249 (PDF p. 284), Appendix B §B.1. Quote: "degree of numerical dispersion" | Do node-count sensitivity before claiming model mismatch. Do not add axial dispersion as a silent patch. | `test_nc_sensitivity_report`; require MTZ node-count note. | Both |
| Adequate nodes inside the MTZ are recommended. | Diss p. 249 (PDF p. 284), Appendix B §B.1. Quote: "adequate number of nodes inside a Mass Transfer Zone" | Add an MTZ/node diagnostic or at least a manifest field documenting `n_c` choice. | `test_nc_manifest_records_mtz_basis`. | Both |
